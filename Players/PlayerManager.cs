/*
* AUTHOR: Ron Bresett
* REFERENCES:
* NOTES:
*   Primary Purpose: Tracks all active players in the game.
*   Responsibilities:
*        • Store list of players
*        • Assign turns
*        • Broadcast eliminations
*   Access Requirements:
*        • Player
*        • AIPlayer

* TODO:
    • Add sorting/access helpers (e.g., get next active player)

* FIXME: [Known bugs or issues]
*/
using UnityEngine;

namespace Salem.Players
{
    public class PlayerManager : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}